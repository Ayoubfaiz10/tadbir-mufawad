# تطبيق تسيير المفوض القضائي — Gestion Huissier

تطبيق ديسكتوب (Electron) لتسيير أعمال المفوضين القضائيين (huissiers de justice). واجهة ثنائية اللغة: **العربية (RTL)** و **الفرنسية (LTR)**، مع مظهر فاتح/داكن.

> ملاحظة: هذه **بنية أساسية (v0.1.0)**. الوظائف المتخصصة للتوليد التلقائي للوثائق القانونية (التبليغ، التنفيذ، المعاينة) ستضاف بعد دراسة متطلبات المهنة.

> **أمان الجلسة:** المصادقة (شاشة الدخول/التسجيل الأول) **معطّلة حالياً بقرار من صاحب المكتب** — التطبيق يفتح بجلسة مدير مباشرة. لا تُوزَّع هذه النسخة على أجهزة مشتركة أو عامة قبل تفعيل المصادقة مجدداً (الكود موجود في `services/auth.js` ويمكن تفعيله من `src/app.js`).

---

## التشغيل

```bash
npm install        # التثبيت (مرة واحدة)
npm start          # تشغيل التطبيق
```

بناء نسخة قابلة للتوزيع (Windows):

```bash
npm run dist       # يولد NSIS Setup + Portable في مجلد dist/
```

الاختبارات الخلفية:

```bash
node tests/backend.test.js   # 311 اختباراً
```

---

## الاعتماديات (الإصدارات المعتمدة)

| الحزمة | الإصدار |
|-------|---------|
| Electron | 43.x |
| electron-builder | 26.x |
| sql.js | 1.14.x |
| archiver | 7.x |

`npm audit`: 0 ثغرات (تشغيل + بناء).

## هيكل المشروع

```
📦 تطبيق مفوض تلقائي
├── main.js                  # العملية الرئيسية: النافذة، القائمة، التخزين (IPC)
├── preload.js               # جسر آمن بين العملية الرئيسية والواجهة (contextBridge)
├── package.json
└── src/
    ├── index.html           # الواجهة (5 صفحات + نافذة منبثقة)
    ├── style.css            # نظام التصميم (متغيرات، RTL/LTR، فاتح/داكن)
    ├── app.js               # منطق الواجهة: اللغة، التنقل، CRUD، النماذج
    └── locales/
        ├── ar.json          # ترجمات العربية
        └── fr.json          # ترجمات الفرنسية
```

---

## الوحدات الحالية

| الوحدة | الحالة |
|-------|--------|
| لوحة التحكم (إحصائيات + آخر الأنشطة + أحدث الملفات) | ✅ جاهزة |
| الملفات القضائية (إضافة/تعديل/حذف/بحث/تصدير CSV) | ✅ جاهزة |
| العملاء (إضافة/تعديل/حذف/بحث/تصدير CSV) | ✅ جاهزة |
| مكتبة النماذج (توليد الوثائق القانونية) | ⏳ قيد الإعداد |
| الإعدادات (بيانات المكتب + اللغة + المظهر) | ✅ جاهزة |

---

## التخزين

تُحفظ البيانات تلقائياً (SQLite) في مجلد بيانات التطبيق:

- Windows: `C:\Users\<المستخدم>\AppData\Roaming\<اسم التطبيق>\data\app.sqlite`

---

## إضافة وحدة جديدة (مثال: توليد الوثائق)

1. **الترجمات**: أضف مفاتيح جديدة في `src/locales/ar.json` و `src/locales/fr.json` تحت قسم `documents`.
2. **الواجهة**: أنشئ الصفحة في `src/index.html` ضمن `<main>`.
3. **المنطق**: أضف الدوال في `src/app.js` واربطها في `bindEvents()`.
4. **الطباعة/PDF**: يمكن استخدام `webContents.printToPDF` في `main.js` مع زر في الواجهة عبر IPC جديد.

---

## إزالة الأخطاء / ملاحظات

- لحل مشكلة «Electron failed to install correctly» بعد `npm install` إذا مُنعت سكربتات التثبيت:
  ```bash
  npm approve-scripts electron
  node node_modules/electron/install.js
  ```

حظاً موفقاً! 🚀
